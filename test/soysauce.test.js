import {expect} from 'chai';
import {SyncObject, SyncMode} from '../app/reducers/StateHolder';

describe('grade 1 math', () => {
  it('1 plus 1 should equal 2', () => {
    expect(1+1).to.equal(2);
  });
});
